{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "deny",
    "permissionDecisionReason": "Removed the force-push flags from git push; retrying as a normal push preserves the requested commits without rewriting the remote. [pack=adapter-command-rewrite, pattern=rewrite-force-push]"
  }
}
