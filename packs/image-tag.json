{
  "id": "image-tag",
  "tool_keywords": [],
  "applies_to": ["*.yaml", "*.yml"],
  "safe_patterns": [],
  "guarded_patterns": [
    {
      "id": "image-tag-latest",
      "type": "content_regex",
      "regex": "(?m)^[ \\t]*-?[ \\t]*image:[ \\t]*[^#\\n]*:latest[ \\t]*$",
      "tier": "tier1",
      "severity": "High",
      "explanation": "Using :latest image tag prevents reproducible deployments",
      "destructive": true,
      "redirect": {
        "channel": "deny",
        "reason_template": "The :latest image tag is banned — it silently changes what runs and makes rollback impossible. Pin this image to semver tag {derived_value}.",
        "rewrite_template": null
      },
      "example_file_path": "regression.yaml",
      "example_content": "image: ronaldraygun/example:latest"
    },
    {
      "id": "image-tag-bare-sha",
      "type": "content_regex",
      "regex": "image: ronaldraygun/[a-z-]+:[0-9a-f]{8,64}",
      "tier": "tier1",
      "severity": "High",
      "explanation": "Bare git SHA image tag prevents reproducible deployments",
      "destructive": true,
      "redirect": {
        "channel": "deny",
        "reason_template": "Never use a bare git SHA for ronaldraygun/* images. Pin this image to semver tag {derived_value}.",
        "rewrite_template": null
      },
      "example_file_path": "regression.yaml",
      "example_content": "image: ronaldraygun/example:0123456789abcdef0123456789abcdef01234567"
    }
  ]
}
