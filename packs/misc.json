{
  "id": "misc",
  "tool_keywords": ["needle"],
  "applies_to": [],
  "safe_patterns": [],
  "guarded_patterns": [
    {
      "id": "needle-cleanup",
      "type": "command_regex",
      "regex": "^needle\\s+cleanup(?:\\s|$)",
      "tier": "tier1",
      "severity": "Critical",
      "explanation": "needle cleanup sends SIGHUP to live workers, interrupting active work and risking incomplete operations or data corruption.",
      "destructive": true,
      "redirect": {
        "channel": "deny",
        "reason_template": "The `needle cleanup` command is blocked because it would SIGHUP live workers. Let workers finish naturally or wait until they are idle before cleaning up.",
        "rewrite_template": null
      }
    }
  ]
}
