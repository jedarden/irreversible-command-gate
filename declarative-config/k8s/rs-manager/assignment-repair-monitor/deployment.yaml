---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assignment-repair-monitor
  namespace: assignment-repair-monitor
  labels:
    app: assignment-repair-monitor
    component: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: assignment-repair-monitor
  template:
    metadata:
      labels:
        app: assignment-repair-monitor
        component: monitoring
    spec:
      serviceAccountName: assignment-repair-monitor
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: monitor
        image: ronaldraygun/assignment-repair-monitor:0.1.0
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        env:
        - name: ICG_WORKSPACE_PATH
          value: /workspace
        - name: ICG_CHECK_INTERVAL_SECONDS
          value: "300"
        - name: ICG_AUTO_REPAIR_ENABLED
          value: "true"
        - name: ICG_MONITOR_HOST
          value: "0.0.0.0"
        - name: ICG_MONITOR_PORT
          value: "9096"
        - name: DB_PATH
          value: "/workspace/.beads/beads.db"
        - name: ASSIGNMENT_TIMEOUT_HOURS
          value: "4"
        ports:
        - name: http
          containerPort: 9096
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        volumeMounts:
        - name: workspace
          mountPath: /workspace
          readOnly: false
        - name: diagnostics
          mountPath: /workspace/.beads/diagnostics
      volumes:
      - name: workspace
        persistentVolumeClaim:
          claimName: bead-workspace-pvc
          readOnly: false
      - name: diagnostics
        emptyDir:
          sizeLimit: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: assignment-repair-monitor
  namespace: assignment-repair-monitor
  labels:
    app: assignment-repair-monitor
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 9096
    targetPort: http
    protocol: TCP
  selector:
    app: assignment-repair-monitor
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: assignment-repair-monitor
  namespace: assignment-repair-monitor
automountServiceAccountToken: false
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bead-workspace-pvc
  namespace: assignment-repair-monitor
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: sata
---
apiVersion: v1
kind: Namespace
metadata:
  name: assignment-repair-monitor
  labels:
    name: assignment-repair-monitor
    component: monitoring
